{
    "devDependencies": {
        "concurrently": "^7.0.0",
        "jest": "^27.5.1",
        "supertest": "^6.2.2"
    },
    "scripts": {
        "webserver": "./webserver",
        "test": "jest",
        "start": "concurrently --kill-others \"npm run webserver\" \"npm run test\"",
        "test:watch": "jest --watch"
    }
}
